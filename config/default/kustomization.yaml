# Default deployment overlay: CRDs + RBAC + operator + apiserver, all in-cluster.
# Apply with:  kubectl apply -k config/default
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../crd
- ../rbac
- ../manager
- ../apiserver
