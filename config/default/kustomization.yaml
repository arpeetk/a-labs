# Default deployment overlay: CRDs + RBAC + manager. Apply with:
#   kubectl apply -k config/default
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../crd
- ../rbac
- ../manager
